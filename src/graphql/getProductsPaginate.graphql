query paginateProd($skipping: Int!, $limit: Int!){
  getAllProductsPaginate(skipping: $skipping, limit: $limit) {
   data {
     id
     title
     stock
     price
     rating
     description
     brand
     categoryName
     images
     dateCreated
     dateCreatedHuman
     coverImage
   }
    err
  }
}
