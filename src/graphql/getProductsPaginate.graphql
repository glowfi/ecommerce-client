query paginateProd($skipping: Int!, $limit: Int!){
  getAllProductsPaginate(skipping: $skipping, limit: $limit) {
   data {
     id
     title
     stock
     price
     description
     brand
     categoryName
     images
     dateCreated
     dateCreatedHuman
     coverImage
   }
    err
  }
}
