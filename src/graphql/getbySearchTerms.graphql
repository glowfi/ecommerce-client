query search_atlas($term: String!){
  getProductsBySearchTermAtlasSearch(term: $term) {
    data {
      brand
      categoryName
      description
      coverImage
      id
      price
      score
      sellerName
      title
    }
  }
}
