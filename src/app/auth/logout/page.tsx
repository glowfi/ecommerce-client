import LogoutForm from '@/components/auth/logout/Logout';
import React from 'react';

const Logout = () => {
    return (
        <>
            <LogoutForm />
        </>
    );
};

export default Logout;
